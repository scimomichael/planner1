const fs = require('fs');

// 1. Replace js/schedule.js entirely
fs.writeFileSync('js/schedule.js', "// \u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\n// SCHEDULE \u2014 visual time blocks, task linking,\n//            edit/move, cross-off, 15-min slots,\n//            12-hour AM/PM, auto timezone,\n//            per-block timezone, auto-scroll to now\n// \u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\nconst Sched = (() => {\n  const SLOT_H   = 28;\n  const SLOT_MIN = 15;\n\n  const BLOCK_TYPES = [\n    {id:'class',   label:'Class',   css:'sb-class'},\n    {id:'meeting', label:'Meeting', css:'sb-meeting'},\n    {id:'study',   label:'Study',   css:'sb-study'},\n    {id:'ec',      label:'EC',      css:'sb-ec'},\n    {id:'free',    label:'Free',    css:'sb-free'},\n    {id:'meal',    label:'Meal',    css:'sb-meal'},\n    {id:'sleep',   label:'Sleep',   css:'sb-sleep'},\n    {id:'work',    label:'Work',    css:'sb-work'},\n    {id:'other',   label:'Other',   css:'sb-other'},\n  ];\n\n  // Hours: 5 AM \u2192 2 AM\n  const HOURS = [...Array.from({length:19},(_,i)=>i+5), 0, 1];\n  const TOTAL_SLOTS = HOURS.length * 4;\n\n  // \u2500\u2500 Timezone \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n  let localTz = Intl.DateTimeFormat().resolvedOptions().timeZone;\n\n  function detectTz() {\n    try {\n      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;\n      if (tz && tz !== localTz) {\n        localTz = tz;\n        Store.toast('Timezone updated: ' + tz);\n        renderBoth();\n      }\n    } catch(e) {}\n  }\n  setInterval(detectTz, 30000);\n\n  // Convert a time string + source tz to local tz time string\n  // e.g. \"14:00\" in \"America/New_York\" \u2192 \"11:00\" if local is LA\n  function convertToLocalTz(timeStr, srcTz) {\n    if (!timeStr || !srcTz || srcTz === localTz) return timeStr;\n    try {\n      // Use today as the date anchor\n      const [h, m] = timeStr.split(':').map(Number);\n      const today = new Date();\n      today.setHours(h, m, 0, 0);\n      // Format in source tz to get ISO, then reformat in local tz\n      const srcFormatter = new Intl.DateTimeFormat('en-CA', {\n        timeZone: srcTz, hour: '2-digit', minute: '2-digit', hour12: false\n      });\n      const localFormatter = new Intl.DateTimeFormat('en-CA', {\n        timeZone: localTz, hour: '2-digit', minute: '2-digit', hour12: false\n      });\n      // Build a date in srcTz at the given time\n      const srcParts = new Intl.DateTimeFormat('en-US', {\n        timeZone: srcTz, year:'numeric', month:'2-digit', day:'2-digit'\n      }).formatToParts(today);\n      const yr  = srcParts.find(p=>p.type==='year').value;\n      const mo  = srcParts.find(p=>p.type==='month').value;\n      const dy  = srcParts.find(p=>p.type==='day').value;\n      const utc = new Date(`${yr}-${mo}-${dy}T${String(h).padStart(2,'0')}:${String(m).padStart(2,'0')}:00`);\n      // Get offset difference\n      const srcOffset  = getOffsetMinutes(srcTz, utc);\n      const localOffset= getOffsetMinutes(localTz, utc);\n      const diffMins   = srcOffset - localOffset;\n      const totalMins  = h*60 + m - diffMins;\n      const newH = ((Math.floor(totalMins/60)) % 24 + 24) % 24;\n      const newM = ((totalMins % 60) + 60) % 60;\n      return `${String(newH).padStart(2,'0')}:${String(newM).padStart(2,'0')}`;\n    } catch(e) { return timeStr; }\n  }\n\n  function getOffsetMinutes(tz, date) {\n    try {\n      const utcStr  = date.toLocaleString('en-US', {timeZone:'UTC'});\n      const tzStr   = date.toLocaleString('en-US', {timeZone:tz});\n      const utcDate = new Date(utcStr);\n      const tzDate  = new Date(tzStr);\n      return (tzDate - utcDate) / 60000;\n    } catch(e) { return 0; }\n  }\n\n  // \u2500\u2500 Time formatting (always 12-hour AM/PM) \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n  function fmt12(h, m) {\n    const ampm = h < 12 ? 'AM' : 'PM';\n    const h12  = h === 0 ? 12 : h > 12 ? h - 12 : h;\n    return `${h12}:${String(m).padStart(2,'0')} ${ampm}`;\n  }\n  function fmtHQ(h, q)   { return fmt12(h, q * 15); }\n  function fmtStr(t)     {\n    if (!t) return '';\n    const [h,m] = t.split(':').map(Number);\n    return fmt12(h, m);\n  }\n  function toMins(t)     {\n    if (!t) return null;\n    const [h,m] = t.split(':').map(Number);\n    return h*60+m;\n  }\n  function fromMins(m)   {\n    return `${String(Math.floor(m/60)%24).padStart(2,'0')}:${String(m%60).padStart(2,'0')}`;\n  }\n\n  function slotTop(h, q) {\n    const idx = HOURS.indexOf(h);\n    return ((idx < 0 ? 0 : idx) * 4 + q) * SLOT_H;\n  }\n  function blockHeight(startMins, endMins) {\n    const diff = (endMins ?? startMins+60) - startMins;\n    return Math.max(SLOT_H, Math.round(Math.max(diff,0)/SLOT_MIN)*SLOT_H);\n  }\n\n  // \u2500\u2500 Offsets \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n  let offset = 0, fullOffset = 0;\n  function dateFor(off) {\n    const d = new Date(Store.today());\n    d.setDate(d.getDate() + off);\n    return d;\n  }\n  function dayLabel(d) {\n    const n = Store.daysUntil(Store.toStr(d));\n    const dow = d.toLocaleDateString('en-US',{weekday:'long'});\n    if (n===0)  return `Today \u00b7 ${dow}`;\n    if (n===1)  return `Tomorrow \u00b7 ${dow}`;\n    if (n===-1) return `Yesterday \u00b7 ${dow}`;\n    return d.toLocaleDateString('en-US',{weekday:'long',month:'short',day:'numeric'});\n  }\n\n  function shift(dir)     { offset+=dir;     render('schedGrid','schedLabel',offset); }\n  function shiftFull(dir) { fullOffset+=dir; render('schedFullGrid','schedFullSub',fullOffset); }\n  function getOffset()     { return offset; }\n  function getFullOffset() { return fullOffset; }\n\n  // \u2500\u2500 Render \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n  function render(gridId, labelId, off) {\n    const d      = dateFor(off);\n    const dk     = Store.toStr(d);\n    const blocks = Store.schedule[dk] || [];\n    const isToday = off === 0 && gridId === 'schedGrid';\n\n    if (labelId) {\n      const el = document.getElementById(labelId);\n      if (el) el.textContent = dayLabel(d);\n    }\n\n    const wrap = document.getElementById(gridId);\n    if (!wrap) return;\n    wrap.innerHTML = '';\n    wrap.style.position = 'relative';\n\n    const totalH = TOTAL_SLOTS * SLOT_H;\n\n    // Time axis\n    const axis = document.createElement('div');\n    axis.className = 'sched-axis';\n    axis.style.height = totalH + 'px';\n    HOURS.forEach((h,i) => {\n      const lbl = document.createElement('div');\n      lbl.className = 'sched-axis-label';\n      lbl.style.top = (i*4*SLOT_H)+'px';\n      lbl.style.transform = i===0 ? 'translateY(0)' : 'translateY(-50%)';\n      lbl.textContent = fmtHQ(h,0);\n      axis.appendChild(lbl);\n    });\n    wrap.appendChild(axis);\n\n    // Scrollable inner container\n    const scroller = document.createElement('div');\n    scroller.className = 'sched-scroller';\n    scroller.style.height = '100%';\n    scroller.style.overflowY = 'auto';\n    scroller.style.position = 'relative';\n\n    const inner = document.createElement('div');\n    inner.style.position = 'relative';\n    inner.style.height = totalH + 'px';\n    inner.style.marginLeft = '68px';\n\n    // Current time line\n    if (isToday) {\n      const now    = new Date();\n      const nowMins= now.getHours()*60 + now.getMinutes();\n      const startMins = 5*60;\n      const endMins   = 26*60; // 2AM next day\n      if (nowMins >= startMins || nowMins <= 120) {\n        const adjMins = nowMins < 120 ? nowMins + 24*60 : nowMins;\n        const pct = (adjMins - startMins) / (endMins - startMins);\n        const topPx = pct * totalH;\n        const line = document.createElement('div');\n        line.className = 'now-line';\n        line.style.top = topPx + 'px';\n        inner.appendChild(line);\n      }\n    }\n\n    // Slot click targets\n    HOURS.forEach((h,hi) => {\n      for (let q=0;q<4;q++) {\n        const slot = document.createElement('div');\n        slot.className = 'sched-slot'+(q===0?' hour-top':'');\n        slot.style.cssText = `position:absolute;left:0;right:0;top:${(hi*4+q)*SLOT_H}px;height:${SLOT_H}px`;\n        slot.addEventListener('click', () => BlockModal.open(dk, h, q));\n        inner.appendChild(slot);\n      }\n    });\n\n    // Blocks\n    blocks.forEach((b, bi) => {\n      // Convert block time to local tz if it has a stored tz\n      const displayStart = b.storedTz ? convertToLocalTz(b.start, b.storedTz) : b.start;\n      const displayEnd   = b.storedTz ? convertToLocalTz(b.end,   b.storedTz) : b.end;\n\n      const startMins = toMins(displayStart);\n      if (startMins === null) return;\n      const endMins   = toMins(displayEnd) ?? startMins+60;\n\n      const sh = Math.floor(startMins/60)%24;\n      const sq = Math.floor((startMins%60)/15);\n      const top    = slotTop(sh, sq);\n      const height = blockHeight(startMins, endMins);\n\n      const linkedTask = b.taskId ? Store.tasks.find(t=>t.id===b.taskId) : null;\n      const timeDisplay = displayStart && displayEnd\n        ? `${fmtStr(displayStart)}\u2013${fmtStr(displayEnd)}`\n        : displayStart ? fmtStr(displayStart) : '';\n      const tzNote = b.storedTz && b.storedTz !== localTz\n        ? `<div class=\"sched-block-tz\">${b.storedTz.split('/').pop().replace('_',' ')}</div>` : '';\n\n      const block = document.createElement('div');\n      block.className = `sched-block ${b.css||'sb-other'}${b.done?' sched-block-done':''}`;\n      block.style.cssText = `top:${top}px;height:${height}px`;\n      block.innerHTML = `\n        <div class=\"sched-block-check${b.done?' done':''}\"\n             onclick=\"event.stopPropagation();Sched.toggleDone('${dk}',${bi})\"></div>\n        <div class=\"sched-block-label\">${Store.esc(b.label)}</div>\n        ${timeDisplay ? `<div class=\"sched-block-time\">${timeDisplay}</div>` : ''}\n        ${tzNote}\n        ${linkedTask ? `<div class=\"sched-block-task\">\u2192 ${Store.esc(linkedTask.name)}</div>` : ''}\n      `;\n      block.addEventListener('click', () => EditBlockModal.open(dk, bi));\n      inner.appendChild(block);\n    });\n\n    scroller.appendChild(inner);\n    wrap.appendChild(scroller);\n\n    // Auto-scroll to current time for today\n    if (isToday) {\n      requestAnimationFrame(() => {\n        const now     = new Date();\n        const nowMins = now.getHours()*60 + now.getMinutes();\n        const startMins = 5*60;\n        const adjMins   = nowMins < 120 ? nowMins+24*60 : nowMins;\n        const pct       = Math.max(0, (adjMins - startMins) / (TOTAL_SLOTS*SLOT_MIN));\n        const scrollTo  = pct * totalH - scroller.clientHeight/2;\n        scroller.scrollTop = Math.max(0, scrollTo);\n      });\n    }\n  }\n\n  function renderBoth() {\n    render('schedGrid','schedLabel',offset);\n    if (document.getElementById('view-schedule')?.classList.contains('active')) {\n      render('schedFullGrid','schedFullSub',fullOffset);\n    }\n  }\n\n  function toggleDone(dk, bi) {\n    const b = Store.schedule[dk];\n    if (!b?.[bi]) return;\n    b[bi].done = !b[bi].done;\n    Store.persist();\n    renderBoth();\n  }\n  function addBlock(dk, block) {\n    if (!Store.schedule[dk]) Store.schedule[dk]=[];\n    Store.schedule[dk].push(block);\n    Store.persist(); renderBoth();\n  }\n  function updateBlock(dk, bi, block) {\n    if (!Store.schedule[dk]) return;\n    Store.schedule[dk][bi]=block;\n    Store.persist(); renderBoth();\n  }\n  function removeBlock(dk, bi) {\n    if (!Store.schedule[dk]) return;\n    Store.schedule[dk].splice(bi,1);\n    Store.persist(); renderBoth();\n  }\n\n  function getBlockTypes()  { return BLOCK_TYPES; }\n  function getLocalTz()     { return localTz; }\n  function fmtTimeStr(t)    { return fmtStr(t); }\n  function minsToTimeStr(m) { return fromMins(m); }\n\n  return {\n    shift, shiftFull, getOffset, getFullOffset,\n    render, renderBoth,\n    addBlock, updateBlock, removeBlock, toggleDone,\n    getBlockTypes, getLocalTz, fmtTimeStr, minsToTimeStr,\n  };\n})();\n\n\n// \u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\n// BLOCK MODAL \u2014 add new block\n// \u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\nconst BlockModal = (() => {\n  let _dk=null, _type='study';\n\n  function setType(t) {\n    _type=t;\n    renderTypeGrid('btypeGrid',t,setType);\n  }\n\n  function open(dk,h,q) {\n    _dk=dk; _type='study';\n    const d = new Date(dk+'T00:00:00');\n    document.getElementById('blockModalTitle').textContent =\n      `Add block \u00b7 ${d.toLocaleDateString('en-US',{weekday:'short',month:'short',day:'numeric'})}`;\n    document.getElementById('bLabel').value='';\n    const startMins=h*60+q*15, endMins=startMins+60;\n    document.getElementById('bStart').value=Sched.minsToTimeStr(startMins);\n    document.getElementById('bEnd').value=Sched.minsToTimeStr(endMins%(24*60));\n    // Set default timezone to local\n    const tzSel=document.getElementById('bTz');\n    if(tzSel) tzSel.value=Sched.getLocalTz();\n    renderTypeGrid('btypeGrid',_type,setType);\n    populateTaskSelect('bTaskLink',null);\n    document.getElementById('blockBackdrop').classList.add('open');\n    setTimeout(()=>document.getElementById('bLabel').focus(),40);\n  }\n\n  function close() { document.getElementById('blockBackdrop').classList.remove('open'); }\n  function bdClick(e) { if(e.target.id==='blockBackdrop') close(); }\n  function save() {\n    const label  = document.getElementById('bLabel').value.trim()||_type;\n    const start  = document.getElementById('bStart').value;\n    const end    = document.getElementById('bEnd').value;\n    const taskId = document.getElementById('bTaskLink').value||null;\n    const storedTz = document.getElementById('bTz')?.value||Sched.getLocalTz();\n    const css    = Sched.getBlockTypes().find(t=>t.id===_type)?.css||'sb-other';\n    Sched.addBlock(_dk,{label,type:_type,css,start,end,taskId,storedTz,done:false});\n    close();\n  }\n  return {open,close,bdClick,save};\n})();\n\n\n// \u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\n// EDIT BLOCK MODAL\n// \u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\nconst EditBlockModal = (() => {\n  let _dk=null,_bi=null,_type='study';\n\n  function setType(t) { _type=t; renderTypeGrid('ebTypeGrid',t,setType); }\n\n  function open(dk,bi) {\n    _dk=dk; _bi=bi;\n    const block=Store.schedule[dk]?.[bi];\n    if(!block) return;\n    _type=block.type||'study';\n    document.getElementById('ebLabel').value=block.label||'';\n    document.getElementById('ebStart').value=block.start||'';\n    document.getElementById('ebEnd').value=block.end||'';\n    document.getElementById('ebDate').value=dk;\n    const tzSel=document.getElementById('ebTz');\n    if(tzSel) tzSel.value=block.storedTz||Sched.getLocalTz();\n    renderTypeGrid('ebTypeGrid',_type,setType);\n    populateTaskSelect('ebTaskLink',block.taskId||null);\n    document.getElementById('editBlockBackdrop').classList.add('open');\n    setTimeout(()=>document.getElementById('ebLabel').focus(),40);\n  }\n\n  function close() { document.getElementById('editBlockBackdrop').classList.remove('open'); }\n  function bdClick(e) { if(e.target.id==='editBlockBackdrop') close(); }\n  function save() {\n    const label  = document.getElementById('ebLabel').value.trim();\n    const start  = document.getElementById('ebStart').value;\n    const end    = document.getElementById('ebEnd').value;\n    const newDk  = document.getElementById('ebDate').value;\n    const taskId = document.getElementById('ebTaskLink').value||null;\n    const storedTz=document.getElementById('ebTz')?.value||Sched.getLocalTz();\n    const css    = Sched.getBlockTypes().find(t=>t.id===_type)?.css||'sb-other';\n    const orig   = Store.schedule[_dk]?.[_bi]||{};\n    const block  = {...orig,label,type:_type,css,start,end,taskId,storedTz};\n    if(newDk!==_dk) { Sched.removeBlock(_dk,_bi); Sched.addBlock(newDk,block); }\n    else Sched.updateBlock(_dk,_bi,block);\n    close();\n  }\n  function del() { Sched.removeBlock(_dk,_bi); close(); }\n  return {open,close,bdClick,save,del};\n})();\n\n\n// \u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\n// SHARED HELPERS\n// \u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\nfunction renderTypeGrid(gridId,activeId,onPick) {\n  const grid=document.getElementById(gridId);\n  if(!grid) return;\n  grid.innerHTML=Sched.getBlockTypes().map(t=>\n    `<button class=\"btype-btn ${t.css}${t.id===activeId?' sel':''}\" data-t=\"${t.id}\">${t.label}</button>`\n  ).join('');\n  grid.querySelectorAll('.btype-btn').forEach(btn=>{\n    btn.addEventListener('click',()=>onPick(btn.dataset.t));\n  });\n}\n\nfunction populateTaskSelect(selectId,selectedId) {\n  const sel=document.getElementById(selectId);\n  if(!sel) return;\n  const active=Store.tasks.filter(t=>t.status!=='Done');\n  sel.innerHTML=`<option value=\"\">\u2014 None \u2014</option>`+\n    active.map(t=>`<option value=\"${t.id}\"${t.id===selectedId?' selected':''}>${Store.esc(t.name.slice(0,55))}</option>`).join('');\n}\n\nfunction buildTzSelect(selectId) {\n  const sel=document.getElementById(selectId);\n  if(!sel) return;\n  // Common timezones grouped\n  const tzList = [\n    'America/New_York','America/Chicago','America/Denver','America/Los_Angeles',\n    'America/Phoenix','America/Anchorage','Pacific/Honolulu',\n    'Europe/London','Europe/Paris','Europe/Berlin','Europe/Moscow',\n    'Asia/Tokyo','Asia/Shanghai','Asia/Kolkata','Asia/Dubai',\n    'Australia/Sydney','Pacific/Auckland','UTC'\n  ];\n  const local = Sched.getLocalTz();\n  const all = [local, ...tzList.filter(t=>t!==local)];\n  sel.innerHTML = all.map(tz => {\n    const label = tz === local ? `${tz} (your timezone)` : tz;\n    return `<option value=\"${tz}\">${label}</option>`;\n  }).join('');\n  sel.value = local;\n}\n", 'utf8');
console.log('✅ js/schedule.js');

// 2. Append new CSS to css/app.css
let css = fs.readFileSync('css/app.css', 'utf8');
if (!css.includes('.sb-meeting')) {
  css += "\n/* \u2500\u2500 Meeting block type \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500 */\n.sb-meeting{background:rgba(13,148,136,.14);border-color:rgba(13,148,136,.28);color:#0f766e}\n@media(prefers-color-scheme:dark){\n  .sb-meeting{background:rgba(13,148,136,.22);border-color:rgba(13,148,136,.4);color:#5eead4}\n}\n.btype-btn.sel.sb-meeting{background:rgba(13,148,136,.1);border-color:#0d9488;color:#0f766e}\n\n/* \u2500\u2500 Current time line \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500 */\n.now-line{\n  position:absolute;left:0;right:0;height:2px;\n  background:var(--red);z-index:4;pointer-events:none;\n}\n.now-line::before{\n  content:'';position:absolute;left:-4px;top:-4px;\n  width:10px;height:10px;border-radius:50%;background:var(--red);\n}\n\n/* \u2500\u2500 Schedule scroller (scroll inside box only) \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500 */\n.sched-wrap{overflow:hidden}\n.sched-scroller{\n  position:absolute;top:0;left:68px;right:0;bottom:0;\n  overflow-y:auto;overflow-x:hidden;\n  scrollbar-width:thin;\n  scrollbar-color:var(--b2) transparent;\n}\n.sched-scroller::-webkit-scrollbar{width:4px}\n.sched-scroller::-webkit-scrollbar-track{background:transparent}\n.sched-scroller::-webkit-scrollbar-thumb{background:var(--b2);border-radius:2px}\n\n/* \u2500\u2500 Timezone label on block \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500 */\n.sched-block-tz{\n  font-size:9px;font-family:var(--mono);opacity:.6;\n  margin-top:1px;white-space:nowrap;\n}\n\n/* \u2500\u2500 Timezone select in modals \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500 */\n.tz-select{\n  width:100%;padding:9px 12px;\n  border:1px solid var(--b2);border-radius:var(--r-sm);\n  background:var(--surf2);color:var(--tx);font-size:13px;outline:none;cursor:pointer;\n  transition:border-color var(--t);\n}\n.tz-select:focus{border-color:var(--blue)}\n";
  fs.writeFileSync('css/app.css', css, 'utf8');
  console.log('✅ css/app.css');
} else {
  console.log('⏭  css/app.css already patched');
}

// 3. Patch index.html — add timezone selects to block modals
let html = fs.readFileSync('index.html', 'utf8');
const bTzField = `      <div class="m-field" style="margin-top:12px">
        <div class="m-lbl">Timezone</div>
        <select class="m-sel tz-select" id="bTz"></select>
      </div>`;
const ebTzField = `      <div class="m-field" style="margin-top:12px">
        <div class="m-lbl">Timezone</div>
        <select class="m-sel tz-select" id="ebTz"></select>
      </div>`;

if (!html.includes('id="bTz"')) {
  html = html.replace(
    `      <div class="m-field" style="margin-top:12px">\n        <div class="m-lbl">Link to task (optional)</div>\n        <select class="m-sel" id="bTaskLink">\n          <option value="">— None —</option>\n        </select>\n      </div>\n    </div>\n    <div class="modal-ft">\n      <button class="btn-ghost" onclick="BlockModal.close()">Cancel</button>\n      <button class="btn-primary" onclick="BlockModal.save()">Add block</button>`,
    `      <div class="m-field" style="margin-top:12px">\n        <div class="m-lbl">Link to task (optional)</div>\n        <select class="m-sel" id="bTaskLink">\n          <option value="">— None —</option>\n        </select>\n      </div>\n` + bTzField + `\n    </div>\n    <div class="modal-ft">\n      <button class="btn-ghost" onclick="BlockModal.close()">Cancel</button>\n      <button class="btn-primary" onclick="BlockModal.save()">Add block</button>`
  );
}
if (!html.includes('id="ebTz"')) {
  html = html.replace(
    `      <div class="m-field" style="margin-top:12px">\n        <div class="m-lbl">Linked task</div>\n        <select class="m-sel" id="ebTaskLink">\n          <option value="">— None —</option>\n        </select>\n      </div>\n    </div>\n    <div class="modal-ft">\n      <button class="btn-delete" onclick="EditBlockModal.del()">Delete</button>`,
    `      <div class="m-field" style="margin-top:12px">\n        <div class="m-lbl">Linked task</div>\n        <select class="m-sel" id="ebTaskLink">\n          <option value="">— None —</option>\n        </select>\n      </div>\n` + ebTzField + `\n    </div>\n    <div class="modal-ft">\n      <button class="btn-delete" onclick="EditBlockModal.del()">Delete</button>`
  );
}
fs.writeFileSync('index.html', html, 'utf8');
console.log('✅ index.html');

// 4. Wire up buildTzSelect on open — patch app.js to call it
let appjs = fs.readFileSync('js/app.js', 'utf8');
if (!appjs.includes('buildTzSelect')) {
  // After DOMContentLoaded opens modals, build tz selects once
  appjs = appjs.replace(
    "window.addEventListener('DOMContentLoaded', () => {",
    "window.addEventListener('DOMContentLoaded', () => {
    // Build tz dropdowns once on load
    setTimeout(() => { buildTzSelect('bTz'); buildTzSelect('ebTz'); }, 200);"
  );
  fs.writeFileSync('js/app.js', appjs, 'utf8');
  console.log('✅ js/app.js');
}

console.log('\n✅ All done. Delete patch.js from your repo.');
